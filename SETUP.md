# Public Storefront Setup

Copy the `docs/` folder into a separate public GitHub repo. Turn on GitHub Pages for that public repo. Do not add `generate_kits.py`, `kits/`, `bundles/`, `reports/`, `payhip/`, or `regulations/` to the public repo.
